language: python

os: linux

dist: xenial

jobs:
 include:
    - name: "Python 3.8.0 on Xenial Linux"
      python: 3.8          


install:
  - pip install -r requirements.txt

virtualenv:
  system_site_packages: true  

script:
   python -m unittest 
